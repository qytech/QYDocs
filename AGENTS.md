# QYDocs 仓库约束

## 适用范围

- 本仓库只维护 QYTech Android SDK 的公开文档、接入示例和更新日志。
- 默认使用简体中文；类名、API、协议名、Maven 坐标等技术标识保持原文。
- 未经明确要求，不修改 SDK 实现，不猜测未提供的 API、版本号、发布日期或兼容性结论。

## 目录与文件

- `README.md` 是文档库首页，负责模块导航、能力摘要和最后更新时间。
- 每个 SDK 使用 `docs/<Module>/index.md` 作为主文档；模块名沿用既有目录大小写。
- 独立更新日志可使用 `docs/<Module>/CHANGELOG.md`。只有确有独立快速接入场景时才增加模块级 `README.md`。
- 仓库级 Codex 技能位于 `.codex/skills/`，维护文档时优先使用 `$qydocs-maintainer`。

## 内容约束

- 事实以用户提供的发布信息、SDK 源码或可信发布源为准；无法核实时明确标注待确认，不自行补全。
- 新版本记录放在旧版本之前，版本号使用小写 `v`，日期使用 `YYYY-MM-DD`。
- Maven 坐标、示例中的版本号、模块首页最新版本和根 `README.md` 信息必须同步。
- 新增或更新模块时，同步检查根 `README.md` 的导航行、模块简介、最后更新日期和仓库最后更新时间。
- 代码示例优先使用可复制的最小示例；Kotlin、Groovy、XML 等代码块语言标识使用小写。
- 示例不得包含真实密钥、令牌、账号、内网地址或客户信息，统一使用明显占位符。
- 相对链接必须从当前文件出发可解析；不要使用本机绝对路径。
- 保留未涉及段落的原意和历史记录。旧文档中的转义符、标题层级等问题只在触及相关段落时渐进修正。

## 推荐结构

新增或重构模块主文档时，按需采用以下顺序，不为空章节占位：

1. 模块名称与一句话简介
2. 依赖与环境要求
3. 功能特性
4. 权限或配置
5. 快速开始与核心 API
6. 生命周期、线程或资源释放要求
7. 注意事项与常见问题
8. 更新日志

## 工作流程

1. 先阅读目标模块全文、根 `README.md` 和相邻模块的写法。
2. 确认版本号、日期、Maven 坐标、API 签名和兼容性信息的来源。
3. 只做任务所需修改，并同步所有重复出现的事实。
4. 运行 `python .codex/skills/qydocs-maintainer/scripts/validate_docs.py`。
5. 检查 `git diff --check` 和 `git diff`，确认没有意外改写历史内容。

## Git 约定

- 不覆盖或回退用户已有改动，不提交与当前任务无关的格式化。
- 提交信息沿用 Conventional Commits，文档更新优先使用 `docs: ...`。
- 除非用户明确要求，不创建提交、不推送远端。
