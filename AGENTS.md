# QYDocs 仓库约束

- 本仓库维护 QYTech Android SDK 的公开文档、接入示例和更新日志；未经明确要求，不修改 SDK 实现。
- 默认使用简体中文，技术标识保持原文。事实以用户发布信息、SDK 源码或可信发布源为准；无法核实则标注待确认，不猜测 API、版本、日期或兼容性。
- 仅修改任务涉及内容；保留其他段落原意、历史记录及用户已有改动。格式问题仅在触及段落时渐进修正。
- 维护或审查 SDK 文档时，优先使用项目 Skill，并必须遵循 [文档流程](.codex/skills/qydocs-maintainer/SKILL.md) 及其适用参考规范；仅维护工具或规则文件时不加载文档专项内容。
- 已读内容在上下文完整可用时复用；文件变化、上下文丢失或明确要求重读时再读。定向搜索，批量执行独立只读检查，仅输出必要结果；不得省略必要阅读、权限、审批、安全要求和验证。
- 遵循现有指令优先级；遇到冲突先定位来源，仅在无法依现有授权安全决定时询问。
- 不覆盖或回退用户改动，不做无关格式化。除非用户明确要求，不提交、不推送；提交采用 Conventional Commits，文档优先 `docs: ...`。
